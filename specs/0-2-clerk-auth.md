# 0-2 — Authentication (Clerk)

## Step

`0-2` · Phase 0 — Foundation

## Status

`skipped`

## Decision

Auth was removed from this learning project. User-specific features (e.g. portfolios) use a single shared data model without sign-in for now. Clerk integration can be reintroduced later if needed.

## Original scope (not shipped)

- Clerk application + env vars
- `@clerk/nextjs` with middleware protecting `/dashboard`
- Sign-in / sign-up pages and auth header

## On completion

Marked `skipped` in [progress.md](./progress.md). See [context/auth.md](../context/auth.md).
