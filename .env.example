# Copy to frontend/.env.local (Next.js reads env from the frontend/ directory)
#
#   cp .env.example frontend/.env.local
#
# Backend API URLs — defaults match native local dev (no Docker required).

NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API_NODE_URL=http://localhost:3001
